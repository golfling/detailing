// import 'package:cloud_firestore/cloud_firestore.dart';
// import 'package:detailing/features/firebase/models/time_stamps/time_stamps.dart';

class TimeInit {
  static final TimeInit instance = TimeInit._();

  TimeInit._();

  // final _database = FirebaseFirestore.instance;

  // void init() {
  //   final dateNow = DateTime.now().add(const Duration(days: 1));
  //   final dateLast = DateTime.now().add(const Duration(days: 8));
  //   final list = <FreeDays>[];
  //   final listHours = <int>[];
  //   for (var i = dateNow; i < dateLast; i += const Duration(days: 1).inMilliseconds) {
  //     for (var i = ; i < count; i++) {
        
  //     }
  //   }
    


  //  final timeStamps = TimeStampsModel(days: [  
  //    FreeDays(freeHours: , day: null)
  //    ]);
  //   _database.collection('free_time').doc('timestamps').set(.toJson());
  // }
}
